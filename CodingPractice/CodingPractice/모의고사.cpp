#include <string>
#include <vector>

using namespace std;

vector<int> solution(vector<int> answers) {
	vector<int> answer;
	vector<int> arr1;
	

	int *arr1 = new int[answers.size()];
	int *arr2 = new int[answers.size()];
	int *arr3 = new int[answers.size()];
	for (int i = 0; i < answers.size(); i++) {
		for (int j = 1; j <= 5; j++)
			arr1[i] == j;
	}
	for (int i = 0; i < answers.size(); i++) {
		for (int j = 1; j <= 5; j++)
			arr1[i] == j;
	}
	for (int i = 0; i < answers.size(); i++) {
		for (int j = 1; j <= 5; j++)
			arr1[i] == j;
	}



	return answer;
}